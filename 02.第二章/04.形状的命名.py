#编写一个程序，从形状的边数来确定形状的名称。读取用户输入的边数，然后将适当的名称作为有意义消息的一部分报告。程序应该支持3~10条边的任意形状。如果输入了此范围之外的多条边，则程序应显示适当的错误消息。
Edge_count = int(input("请输入边数 (3-10): "))

shape_names = {
    3: "三角形",
    4: "四边形",
    5: "五边形",
    6: "六边形",
    7: "七边形",
    8: "八边形",
    9: "九边形",
    10: "十边形"
}

# 1. 检查是否在支持的 3 到 10 边范围内
if Edge_count in shape_names:
    print(f"您输入的是 {Edge_count} 条边，对应的形状是：{shape_names[Edge_count]}。")

# 2. 处理超出支持范围的边数
elif Edge_count > 10:
    print(f"抱歉，您输入的边数 {Edge_count} 超出了程序目前支持的范围（最大支持 10 边形）。")

# 3. 处理不构成形状的边数（0, 1, 2 或负数）
elif Edge_count >= 0 and Edge_count < 3:
    print(f"您输入的边数 {Edge_count} 不足以构成一个闭合的平面多边形。请重新输入。")

# 4. 极端情况（如输入负数）
else:
    print("输入的边数必须是正整数。")
