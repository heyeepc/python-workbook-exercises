# 编写一个函数，该函数将一个包含数学表达式的字符串作为其唯一参数，并将其分解为一个标记列表。每个标记应该是括号、操作符或数字。为简单起见，这个练习只处理整数。返回标记列表作为函数的唯一结果。
# 可以假设，传递给函数的字符串总是包含一个有效的数学表达式，该表达式由括号、操作符和整数组成。但是，函数必须处理这些元素之间的可变空白（包括没有空白）。
# 包含一个main程序，通过从用户处读取表达式并打印标记列表来演示标记函数。确保在将包含解答的文件导入另一个程序时，main程序不会运行。
